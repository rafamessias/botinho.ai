{
  "name": "botinho-ws-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec tsx src/server.ts",
    "prisma:sync": "node scripts/sync-prisma.cjs",
    "prisma:generate": "npm run prisma:sync && prisma generate --schema prisma/schema.prisma",
    "postinstall": "npm run prisma:sync"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.10",
    "prisma": "^6.18.0",
    "ws": "^8.18.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  }
}